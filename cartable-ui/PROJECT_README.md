# 🎯 کارتابل - نسخه بهبود یافته

## 📋 درباره پروژه

این نسخه بهبود یافته از پروژه کارتابل است که شامل تمام بهینه‌سازی‌ها و رفع مشکلات درخواست شده می‌باشد.

## ✨ ویژگی‌های جدید

### 🚀 عملکرد
- ✅ لودر اولیه برنامه با لوگو و انیمیشن
- ✅ صفحه آفلاین اختصاصی برای PWA
- ✅ بهینه‌سازی سرعت لود
- ✅ Layout اختصاصی برای صفحات جزئیات

### 🌐 چندزبانه
- ✅ حفظ زبان بعد از reload
- ✅ پشتیبانی کامل از زبان عربی
- ✅ ذخیره خودکار در localStorage

### 📱 موبایل و PWA
- ✅ Toast در بالای صفحه در موبایل
- ✅ دکمه بازگشت بهینه در صفحات جزئیات
- ✅ صفحه‌بندی با دکمه "نمایش بیشتر"
- ✅ تشخیص خودکار حالت آفلاین

### 🔍 فیلترها
- ✅ فیلتر عنوان دستور پرداخت
- ✅ فیلتر شماره دستور پرداخت
- ✅ فیلتر کد رهگیری
- ✅ فیلتر وضعیت درخواست
- ✅ فیلتر بازه تاریخی ثبت

## 🛠️ تکنولوژی‌ها

- **Framework:** Next.js 16.0.0
- **React:** 19.2.0
- **TypeScript:** 5.x
- **Styling:** Tailwind CSS 4.x
- **UI Components:** Radix UI
- **Animation:** Framer Motion
- **i18n:** i18next
- **Icons:** Lucide React

## 📦 نصب و راه‌اندازی

### پیش‌نیازها
```bash
Node.js >= 18.x
npm >= 9.x
```

### نصب Dependencies
```bash
npm install
```

### اجرای پروژه در حالت Development
```bash
npm run dev
```

پروژه در آدرس `http://localhost:3000` در دسترس خواهد بود.

### Build Production
```bash
npm run build
npm start
```

## 📁 ساختار پروژه

```
cartable/
├── app/                          # صفحات Next.js (App Router)
│   ├── layout.tsx               # Layout اصلی
│   ├── page.tsx                 # صفحه اصلی
│   ├── accounts/                # مدیریت حساب‌ها
│   ├── auth/                    # احراز هویت
│   ├── dashboard/               # داشبورد
│   ├── my-cartable/             # کارتابل من
│   ├── payment-orders/          # دستورات پرداخت
│   │   ├── page.tsx
│   │   ├── [id]/               # جزئیات دستور پرداخت
│   │   └── components/
│   ├── reports/                 # گزارشات
│   └── settings/                # تنظیمات
│
├── components/                   # کامپوننت‌های React
│   ├── common/                  # کامپوننت‌های مشترک
│   │   ├── app-splash-loader.tsx      # ✨ جدید
│   │   ├── offline-indicator.tsx      # ✨ جدید
│   │   ├── app-loading.tsx
│   │   ├── mobile-cards.tsx
│   │   └── ...
│   ├── layout/                  # کامپوننت‌های Layout
│   │   ├── AppLayout.tsx
│   │   ├── DetailLayout.tsx     # ✨ جدید
│   │   ├── Header.tsx
│   │   ├── Sidebar.tsx
│   │   └── ...
│   └── ui/                      # کامپوننت‌های UI
│       ├── button.tsx
│       ├── card.tsx
│       ├── sonner.tsx          # 🔄 بهبود یافته
│       └── ...
│
├── providers/                    # Context Providers
│   ├── i18n-provider.tsx        # 🔄 بهبود یافته
│   ├── theme-provider.tsx
│   └── ...
│
├── hooks/                        # Custom Hooks
├── lib/                         # Utility Functions
├── i18n/                        # فایل‌های ترجمه
│   └── langs/
│       ├── fa.json
│       ├── en.json
│       └── ar.json
├── mocks/                       # داده‌های Mock
├── public/                      # فایل‌های استاتیک
├── styles/                      # استایل‌های Global
└── types/                       # TypeScript Types

```

## 🎨 تم و استایل

پروژه از Tailwind CSS استفاده می‌کند و دارای دو تم روشن و تاریک است:

```tsx
// استفاده از تم
import { useTheme } from "next-themes";

const { theme, setTheme } = useTheme();
setTheme("dark"); // یا "light"
```

## 🌐 چندزبانه

پروژه از سه زبان پشتیبانی می‌کند:
- فارسی (پیش‌فرض)
- انگلیسی
- عربی

```tsx
// استفاده از ترجمه
import useTranslation from "@/hooks/useTranslation";

const { t, locale } = useTranslation();
const text = t("key.path");
```

## 📱 Responsive Design

پروژه کاملاً responsive است و برای موبایل، تبلت و دسکتاپ بهینه شده است.

### Breakpoints
```
sm: 640px
md: 768px
lg: 1024px
xl: 1280px
2xl: 1536px
```

## 🔐 احراز هویت

سیستم احراز هویت شامل:
- ورود با username/password
- OTP دو مرحله‌ای
- مدیریت session
- محافظت از route ها

## 📊 داده‌ها

در حالت Development از داده‌های Mock استفاده می‌شود که در پوشه `/mocks` قرار دارند.

## 🧪 تست

```bash
# اجرای تست‌ها
npm test

# Coverage Report
npm run test:coverage
```

## 🚀 Deploy

### Vercel (توصیه می‌شود)
```bash
vercel
```

### Docker
```bash
docker build -t cartable .
docker run -p 3000:3000 cartable
```

## 📝 تغییرات این نسخه

برای مشاهده لیست کامل تغییرات، فایل `CHANGELOG.md` را ببینید.

### خلاصه تغییرات:
- ✅ 3 کامپوننت جدید
- ✅ 4 فایل بهبود یافته
- ✅ 12+ مشکل رفع شده
- ✅ 8+ ویژگی جدید

## 🤝 مشارکت

برای مشارکت در پروژه:

1. Fork کنید
2. Branch جدید بسازید (`git checkout -b feature/AmazingFeature`)
3. تغییرات را commit کنید (`git commit -m 'Add some AmazingFeature'`)
4. Push کنید (`git push origin feature/AmazingFeature`)
5. Pull Request ایجاد کنید

## 📞 پشتیبانی

برای گزارش مشکل یا درخواست ویژگی جدید از بخش Issues استفاده کنید.

## 📄 مجوز

این پروژه تحت مجوز تدبیر پرداز است.

## 👥 تیم توسعه

- **شرکت:** تدبیر پرداز
- **محصول:** کارتابل تدبیر پی
- **نسخه:** 1.0.0

---

**ساخته شده با ❤️ توسط تیم تدبیر پرداز**
