# خلاصه تغییرات اعمال شده در پروژه کارتابل

## ✅ تغییرات کامل شده

### 1. برنامه
- ✅ کامپوننت لودینگ برنامه با لوگو (`AppSplashLoader`)
- ✅ کامپوننت Offline برای PWA (`OfflineIndicator`)
- ✅ Toaster در موبایل بالای صفحه
- ✅ Layout اختصاصی برای صفحات جزئیات (`DetailLayout`)

### 2. زبان
- ✅ رفع مشکل ذخیره و بازیابی زبان در localStorage
- ✅ رفع مشکل زبان عربی با اضافه کردن `supportedLngs`
- ✅ ذخیره خودکار زبان در تغییرات

### 3. فیلترها
- ✅ اضافه شدن فیلدهای جدید به فیلتر دستور پرداخت:
  - عنوان دستور پرداخت
  - شماره دستور پرداخت  
  - کد رهگیری
  - وضعیت درخواست (قبلاً موجود بود)
  - بازه تاریخی ثبت (قبلاً موجود بود)

### 4. صفحه دستورات پرداخت
- ✅ لاجیک فیلتر برای فیلدهای جدید
- ✅ صفحه‌بندی در موبایل با دکمه "نمایش بیشتر"
- ✅ آماده برای اضافه کردن لوگوی بانک و Pin

## 📝 فایل‌های ایجاد/ویرایش شده

### فایل‌های جدید:
1. `/components/common/app-splash-loader.tsx` - لودر اولیه برنامه
2. `/components/common/offline-indicator.tsx` - صفحه آفلاین PWA
3. `/components/layout/DetailLayout.tsx` - Layout برای صفحات جزئیات

### فایل‌های ویرایش شده:
1. `/providers/i18n-provider.tsx` - رفع مشکلات زبان
2. `/components/ui/sonner.tsx` - نمایش toast در بالا در موبایل
3. `/app/payment-orders/components/filter-sheet.tsx` - فیلترهای جدید
4. `/app/payment-orders/page.tsx` - لاجیک فیلتر و صفحه‌بندی

## 🚧 کارهای باقی‌مانده

### جزئیات دستور پرداخت
- [ ] کاهش سایز هدر
- [ ] کامپوننت تراکنش‌ها با فیلتر و صفحه‌بندی کامل
- [ ] دایالوگ جزئیات تراکنش (مشابه HTML ارسالی)
- [ ] نمایش نوع تراکنش و لوگوی بانک

### سایر موارد  
- [ ] بهینه‌سازی سرعت لود
- [ ] رفع مشکل سوئیچ تم در موبایل
- [ ] Drawer برای انتخاب گروه حساب
- [ ] Pin برای ستون‌های جدول
- [ ] لوگوی بانک در کارت‌ها و جداول

## 📌 نکات مهم برای ادامه کار

1. برای نمایش لوگوی بانک، نیاز به mapping کد بانک به تصویر است
2. برای Pin ستون‌ها، باید state management اضافه شود
3. Transaction detail dialog باید دقیقاً مشابه HTML ارسالی طراحی شود
4. برای account group selector drawer، از Vaul استفاده شود
